export default function Loading() {
  return (
    <div className="flex items-center justify-center w-full h-screen bg-black text-[#576EB2]">
      <span className="animate-pulse">Loading network scene...</span>
    </div>
  );
}